import { css } from "styled-components";

const main = {
  colors: {
    baseColor: "#215DE0"
  },
  cardBaseStyle: css`
    background-color: white;
    box-shadow: 0px 3px 8px 0px rgba(0, 0, 0, 0.07);
  `
};

export default main;
