import { colors } from "../../theme";

const buttonThemeStyles = {
  fontSize: {
    small: "0.6em",
    normal: "0.8em",
    large: "0.1em"
  },
  mainColor: {
    default: colors.variants.default,
    success: colors.variants.success,
    danger: colors.variants.danger,
    warning: colors.variants.warning
  }
};

export default buttonThemeStyles;
